apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: genesys-integration-wizard
  annotations:
    kubernetes.io/ingress.class: "nginx"
spec:
  rules:
  - http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: genesys-integration-wizard
            port:
              number: 80